name: Secrets
on: push
jobs:
  secret-env-vars:
    runs-on: ubuntu-latest
    steps:
      - name: Secrets
         
        env: 
            MONGO_URI: ${{ secrets.MONGO_URI }}
            PORT: ${{ secrets.PORT }}
            TOKEN_SECRET: ${{ secrets.TOKEN_SECRET }}